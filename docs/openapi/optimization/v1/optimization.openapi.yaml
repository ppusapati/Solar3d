openapi: 3.1.0
info:
  title: optimization.v1
paths:
  /optimization.v1.OptimizationService/GeneticAlgorithm:
    post:
      tags:
        - optimization.v1.OptimizationService
      summary: GeneticAlgorithm
      operationId: optimization.v1.OptimizationService.GeneticAlgorithm
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/optimization.v1.GARequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/optimization.v1.GAResponse'
  /optimization.v1.OptimizationService/MonteCarloSampling:
    post:
      tags:
        - optimization.v1.OptimizationService
      summary: MonteCarloSampling
      operationId: optimization.v1.OptimizationService.MonteCarloSampling
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/optimization.v1.MonteCarloRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/optimization.v1.MonteCarloResponse'
  /optimization.v1.OptimizationService/ParetoFrontier:
    post:
      tags:
        - optimization.v1.OptimizationService
      summary: ParetoFrontier
      operationId: optimization.v1.OptimizationService.ParetoFrontier
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/optimization.v1.AddToFrontierRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/optimization.v1.ParetoFrontierResponse'
  /optimization.v1.OptimizationService/ParticleSwarmOptimization:
    post:
      tags:
        - optimization.v1.OptimizationService
      summary: ParticleSwarmOptimization
      operationId: optimization.v1.OptimizationService.ParticleSwarmOptimization
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/optimization.v1.PSORequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/optimization.v1.PSOResponse'
  /optimization.v1.OptimizationService/SimulatedAnnealing:
    post:
      tags:
        - optimization.v1.OptimizationService
      summary: SimulatedAnnealing
      operationId: optimization.v1.OptimizationService.SimulatedAnnealing
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/optimization.v1.SARequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/optimization.v1.SAResponse'
components:
  schemas:
    common.v1.ApiVersion:
      type: object
      properties:
        major:
          type: integer
          title: major
        minor:
          type: integer
          title: minor
        patch:
          type: integer
          title: patch
      title: ApiVersion
      additionalProperties: false
      description: Semantic version carried in API payloads for compatibility checks.
    common.v1.ContractMetadata:
      type: object
      properties:
        schemaVersion:
          title: schema_version
          $ref: '#/components/schemas/common.v1.ApiVersion'
        schemaId:
          type: string
          title: schema_id
        schemaHash:
          type: string
          title: schema_hash
        producer:
          type: string
          title: producer
        correlationId:
          type: string
          title: correlation_id
        createdAt:
          title: created_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: ContractMetadata
      additionalProperties: false
      description: Metadata shared across contracts to support tracing and lineage.
    common.v1.NumericRange:
      type: object
      properties:
        min:
          type: number
          title: min
          format: double
        max:
          type: number
          title: max
          format: double
      title: NumericRange
      additionalProperties: false
    connect-protocol-version:
      type: number
      title: Connect-Protocol-Version
      enum:
        - 1
      description: Define the version of the Connect protocol
      const: 1
    connect-timeout-header:
      type: number
      title: Connect-Timeout-Ms
      description: Define the timeout, in ms
    connect.error:
      type: object
      properties:
        code:
          type: string
          examples:
            - not_found
          enum:
            - canceled
            - unknown
            - invalid_argument
            - deadline_exceeded
            - not_found
            - already_exists
            - permission_denied
            - resource_exhausted
            - failed_precondition
            - aborted
            - out_of_range
            - unimplemented
            - internal
            - unavailable
            - data_loss
            - unauthenticated
          description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
        details:
          type: array
          items:
            $ref: '#/components/schemas/connect.error_details.Any'
          description: A list of messages that carry the error details. There is no limit on the number of messages.
      title: Connect Error
      additionalProperties: true
      description: 'Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation'
    connect.error_details.Any:
      type: object
      properties:
        type:
          type: string
          description: 'A URL that acts as a globally unique identifier for the type of the serialized message. For example: `type.googleapis.com/google.rpc.ErrorInfo`. This is used to determine the schema of the data in the `value` field and is the discriminator for the `debug` field.'
        value:
          type: string
          format: binary
          description: The Protobuf message, serialized as bytes and base64-encoded. The specific message type is identified by the `type` field.
        debug:
          oneOf:
            - type: object
              title: Any
              additionalProperties: true
              description: Detailed error information.
          discriminator:
            propertyName: type
          title: Debug
          description: Deserialized error detail payload. The 'type' field indicates the schema. This field is for easier debugging and should not be relied upon for application logic.
      additionalProperties: true
      description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message, with an additional debug field for ConnectRPC error details.
    google.protobuf.Timestamp:
      type: string
      examples:
        - "2023-01-15T01:30:15.01Z"
        - "2024-12-25T12:00:00Z"
      format: date-time
      description: |-
        A Timestamp represents a point in time independent of any time zone or local
         calendar, encoded as a count of seconds and fractions of seconds at
         nanosecond resolution. The count is relative to an epoch at UTC midnight on
         January 1, 1970, in the proleptic Gregorian calendar which extends the
         Gregorian calendar backwards to year one.

         All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
         second table is needed for interpretation, using a [24-hour linear
         smear](https://developers.google.com/time/smear).

         The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
         restricting to that range, we ensure that we can convert to and from [RFC
         3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

         # Examples

         Example 1: Compute Timestamp from POSIX `time()`.

             Timestamp timestamp;
             timestamp.set_seconds(time(NULL));
             timestamp.set_nanos(0);

         Example 2: Compute Timestamp from POSIX `gettimeofday()`.

             struct timeval tv;
             gettimeofday(&tv, NULL);

             Timestamp timestamp;
             timestamp.set_seconds(tv.tv_sec);
             timestamp.set_nanos(tv.tv_usec * 1000);

         Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

             FILETIME ft;
             GetSystemTimeAsFileTime(&ft);
             UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

             // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
             // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
             Timestamp timestamp;
             timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
             timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

         Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

             long millis = System.currentTimeMillis();

             Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
                 .setNanos((int) ((millis % 1000) * 1000000)).build();

         Example 5: Compute Timestamp from Java `Instant.now()`.

             Instant now = Instant.now();

             Timestamp timestamp =
                 Timestamp.newBuilder().setSeconds(now.getEpochSecond())
                     .setNanos(now.getNano()).build();

         Example 6: Compute Timestamp from current time in Python.

             timestamp = Timestamp()
             timestamp.GetCurrentTime()

         # JSON Mapping

         In JSON format, the Timestamp type is encoded as a string in the
         [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
         format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
         where {year} is always expressed using four digits while {month}, {day},
         {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
         seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
         are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
         is required. A proto3 JSON serializer should always use UTC (as indicated by
         "Z") when printing the Timestamp type and a proto3 JSON parser should be
         able to accept both UTC and other timezones (as indicated by an offset).

         For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
         01:30 UTC on January 15, 2017.

         In JavaScript, one can convert a Date object to this format using the
         standard
         [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
         method. In Python, a standard `datetime.datetime` object can be converted
         to this format using
         [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
         the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
         the Joda Time's [`ISODateTimeFormat.dateTime()`](
         http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
         ) to obtain a formatter capable of generating timestamps in this format.
    optimization.v1.AddToFrontierRequest:
      type: object
      properties:
        objectives:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.Objective'
          title: objectives
        newSolution:
          title: new_solution
          $ref: '#/components/schemas/optimization.v1.Solution'
        input:
          title: input
          $ref: '#/components/schemas/optimization.v1.MultiObjectiveOptimizationInput'
      title: AddToFrontierRequest
      additionalProperties: false
    optimization.v1.CandidateSolution:
      type: object
      properties:
        candidateId:
          type: string
          title: candidate_id
        assignments:
          type: object
          title: assignments
          additionalProperties:
            type: number
            title: value
            format: double
        objectiveValues:
          type: object
          title: objective_values
          additionalProperties:
            type: number
            title: value
            format: double
        constraintViolations:
          type: object
          title: constraint_violations
          additionalProperties:
            type: number
            title: value
            format: double
      title: CandidateSolution
      additionalProperties: false
    optimization.v1.CandidateSolution.AssignmentsEntry:
      type: object
      properties:
        key:
          type: string
          title: key
        value:
          type: number
          title: value
          format: double
      title: AssignmentsEntry
      additionalProperties: false
    optimization.v1.CandidateSolution.ConstraintViolationsEntry:
      type: object
      properties:
        key:
          type: string
          title: key
        value:
          type: number
          title: value
          format: double
      title: ConstraintViolationsEntry
      additionalProperties: false
    optimization.v1.CandidateSolution.ObjectiveValuesEntry:
      type: object
      properties:
        key:
          type: string
          title: key
        value:
          type: number
          title: value
          format: double
      title: ObjectiveValuesEntry
      additionalProperties: false
    optimization.v1.ConstraintDefinition:
      type: object
      properties:
        name:
          type: string
          title: name
        expression:
          type: string
          title: expression
        allowedRange:
          title: allowed_range
          $ref: '#/components/schemas/common.v1.NumericRange'
        hardConstraint:
          type: boolean
          title: hard_constraint
      title: ConstraintDefinition
      additionalProperties: false
    optimization.v1.DecisionVariable:
      type: object
      properties:
        name:
          type: string
          title: name
        variableType:
          title: variable_type
          $ref: '#/components/schemas/optimization.v1.VariableType'
        bounds:
          title: bounds
          $ref: '#/components/schemas/common.v1.NumericRange'
      title: DecisionVariable
      additionalProperties: false
    optimization.v1.Distribution:
      type: object
      properties:
        mean:
          type: number
          title: mean
          format: double
        stdDev:
          type: number
          title: std_dev
          format: double
      title: Distribution
      additionalProperties: false
    optimization.v1.DistributionDefinition:
      type: object
      properties:
        name:
          type: string
          title: name
        distribution:
          title: distribution
          $ref: '#/components/schemas/optimization.v1.DistributionModel'
      title: DistributionDefinition
      additionalProperties: false
    optimization.v1.DistributionModel:
      type: object
      properties:
        mean:
          type: number
          title: mean
          format: double
        stdDev:
          type: number
          title: std_dev
          format: double
      title: DistributionModel
      additionalProperties: false
    optimization.v1.GARequest:
      type: object
      properties:
        populationSize:
          type: integer
          title: population_size
          format: int32
        generations:
          type: integer
          title: generations
          format: int32
        crossoverRate:
          type: number
          title: crossover_rate
          format: double
        mutationRate:
          type: number
          title: mutation_rate
          format: double
        eliteCount:
          type: integer
          title: elite_count
          format: int32
        seed:
          type:
            - integer
            - string
          title: seed
          format: int64
        initialPopulation:
          type: array
          items:
            type: number
            format: double
          title: initial_population
        problem:
          title: problem
          $ref: '#/components/schemas/optimization.v1.OptimizationProblem'
      title: GARequest
      additionalProperties: false
    optimization.v1.GAResponse:
      type: object
      properties:
        bestFitness:
          type: number
          title: best_fitness
          format: double
        bestGenes:
          type: array
          items:
            type: number
            format: double
          title: best_genes
        generationsCompleted:
          type: integer
          title: generations_completed
          format: int32
      title: GAResponse
      additionalProperties: false
    optimization.v1.MonteCarloRequest:
      type: object
      properties:
        distributions:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.Distribution'
          title: distributions
        numSamples:
          type: integer
          title: num_samples
          format: int32
        seed:
          type:
            - integer
            - string
          title: seed
          format: int64
        uncertaintyModel:
          title: uncertainty_model
          $ref: '#/components/schemas/optimization.v1.UncertaintyModel'
      title: MonteCarloRequest
      additionalProperties: false
    optimization.v1.MonteCarloResponse:
      type: object
      properties:
        mean:
          type: number
          title: mean
          format: double
        stdDev:
          type: number
          title: std_dev
          format: double
        p10:
          type: number
          title: p10
          format: double
        p50:
          type: number
          title: p50
          format: double
        p90:
          type: number
          title: p90
          format: double
        minValue:
          type: number
          title: min_value
          format: double
        maxValue:
          type: number
          title: max_value
          format: double
      title: MonteCarloResponse
      additionalProperties: false
    optimization.v1.MultiObjectiveOptimizationInput:
      type: object
      properties:
        problem:
          title: problem
          $ref: '#/components/schemas/optimization.v1.OptimizationProblem'
        initialCandidates:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.CandidateSolution'
          title: initial_candidates
      title: MultiObjectiveOptimizationInput
      additionalProperties: false
    optimization.v1.Objective:
      type: object
      properties:
        name:
          type: string
          title: name
        kind:
          title: kind
          $ref: '#/components/schemas/optimization.v1.Objective.ObjectiveKind'
        weight:
          type: number
          title: weight
          format: double
      title: Objective
      additionalProperties: false
    optimization.v1.Objective.ObjectiveKind:
      type: string
      title: ObjectiveKind
      enum:
        - MINIMIZE
        - MAXIMIZE
    optimization.v1.ObjectiveDefinition:
      type: object
      properties:
        name:
          type: string
          title: name
        kind:
          title: kind
          $ref: '#/components/schemas/optimization.v1.ObjectiveKind'
        weight:
          type: number
          title: weight
          format: double
      title: ObjectiveDefinition
      additionalProperties: false
    optimization.v1.ObjectiveKind:
      type: string
      title: ObjectiveKind
      enum:
        - OBJECTIVE_KIND_UNSPECIFIED
        - OBJECTIVE_KIND_MINIMIZE
        - OBJECTIVE_KIND_MAXIMIZE
    optimization.v1.OptimizationProblem:
      type: object
      properties:
        problemId:
          type: string
          title: problem_id
        variables:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.DecisionVariable'
          title: variables
        objectives:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.ObjectiveDefinition'
          title: objectives
        constraints:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.ConstraintDefinition'
          title: constraints
        randomSeed:
          type:
            - integer
            - string
          title: random_seed
          format: int64
        maxIterations:
          type: integer
          title: max_iterations
          format: int32
        contract:
          title: contract
          $ref: '#/components/schemas/common.v1.ContractMetadata'
      title: OptimizationProblem
      additionalProperties: false
    optimization.v1.PSORequest:
      type: object
      properties:
        numParticles:
          type: integer
          title: num_particles
          format: int32
        iterations:
          type: integer
          title: iterations
          format: int32
        c1:
          type: number
          title: c1
          format: double
        c2:
          type: number
          title: c2
          format: double
        w:
          type: number
          title: w
          format: double
        boundaryMin:
          type: number
          title: boundary_min
          format: double
        boundaryMax:
          type: number
          title: boundary_max
          format: double
        seed:
          type:
            - integer
            - string
          title: seed
          format: int64
        problem:
          title: problem
          $ref: '#/components/schemas/optimization.v1.OptimizationProblem'
      title: PSORequest
      additionalProperties: false
    optimization.v1.PSOResponse:
      type: object
      properties:
        bestPosition:
          type: number
          title: best_position
          format: double
        bestValue:
          type: number
          title: best_value
          format: double
        iterationsCompleted:
          type: integer
          title: iterations_completed
          format: int32
      title: PSOResponse
      additionalProperties: false
    optimization.v1.ParetoFrontierResponse:
      type: object
      properties:
        solutions:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.Solution'
          title: solutions
        frontierSize:
          type: integer
          title: frontier_size
          format: int32
        solutionIds:
          type: array
          items:
            type: integer
            format: int32
          title: solution_ids
      title: ParetoFrontierResponse
      additionalProperties: false
    optimization.v1.SARequest:
      type: object
      properties:
        initialTemperature:
          type: number
          title: initial_temperature
          format: double
        coolingRate:
          type: number
          title: cooling_rate
          format: double
        iterations:
          type: integer
          title: iterations
          format: int32
        perturbationScale:
          type: number
          title: perturbation_scale
          format: double
        seed:
          type:
            - integer
            - string
          title: seed
          format: int64
        initialSolution:
          type: array
          items:
            type: number
            format: double
          title: initial_solution
        problem:
          title: problem
          $ref: '#/components/schemas/optimization.v1.OptimizationProblem'
      title: SARequest
      additionalProperties: false
    optimization.v1.SAResponse:
      type: object
      properties:
        bestEnergy:
          type: number
          title: best_energy
          format: double
        bestSolution:
          type: array
          items:
            type: number
            format: double
          title: best_solution
        finalTemperature:
          type: number
          title: final_temperature
          format: double
      title: SAResponse
      additionalProperties: false
    optimization.v1.Solution:
      type: object
      properties:
        id:
          type: integer
          title: id
          format: int32
        variables:
          type: array
          items:
            type: number
            format: double
          title: variables
        objectives:
          type: array
          items:
            type: number
            format: double
          title: objectives
        rank:
          type: number
          title: rank
          format: double
        crowdingDistance:
          type: number
          title: crowding_distance
          format: double
      title: Solution
      additionalProperties: false
    optimization.v1.UncertaintyModel:
      type: object
      properties:
        distributions:
          type: array
          items:
            $ref: '#/components/schemas/optimization.v1.DistributionDefinition'
          title: distributions
        sampleCount:
          type: integer
          title: sample_count
          format: int32
        randomSeed:
          type:
            - integer
            - string
          title: random_seed
          format: int64
        contract:
          title: contract
          $ref: '#/components/schemas/common.v1.ContractMetadata'
      title: UncertaintyModel
      additionalProperties: false
    optimization.v1.VariableType:
      type: string
      title: VariableType
      enum:
        - VARIABLE_TYPE_UNSPECIFIED
        - VARIABLE_TYPE_CONTINUOUS
        - VARIABLE_TYPE_INTEGER
        - VARIABLE_TYPE_BINARY
security: []
tags:
  - name: optimization.v1.OptimizationService
