openapi: 3.1.0
info:
  title: project.v1
paths:
  /project.v1.ProjectService/CreateProject:
    post:
      tags:
        - project.v1.ProjectService
      summary: CreateProject
      operationId: project.v1.ProjectService.CreateProject
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.CreateProjectRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.CreateProjectResponse'
  /project.v1.ProjectService/DeleteProject:
    post:
      tags:
        - project.v1.ProjectService
      summary: DeleteProject
      operationId: project.v1.ProjectService.DeleteProject
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.DeleteProjectRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.DeleteProjectResponse'
  /project.v1.ProjectService/GetPhaseState:
    post:
      tags:
        - project.v1.ProjectService
      summary: GetPhaseState
      operationId: project.v1.ProjectService.GetPhaseState
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.GetPhaseStateRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.GetPhaseStateResponse'
  /project.v1.ProjectService/GetProject:
    post:
      tags:
        - project.v1.ProjectService
      summary: GetProject
      operationId: project.v1.ProjectService.GetProject
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.GetProjectRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.GetProjectResponse'
  /project.v1.ProjectService/ListPhaseTransitions:
    post:
      tags:
        - project.v1.ProjectService
      summary: ListPhaseTransitions
      operationId: project.v1.ProjectService.ListPhaseTransitions
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.ListPhaseTransitionsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.ListPhaseTransitionsResponse'
  /project.v1.ProjectService/ListProjects:
    post:
      tags:
        - project.v1.ProjectService
      summary: ListProjects
      operationId: project.v1.ProjectService.ListProjects
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.ListProjectsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.ListProjectsResponse'
  /project.v1.ProjectService/TransitionPhase:
    post:
      tags:
        - project.v1.ProjectService
      summary: TransitionPhase
      operationId: project.v1.ProjectService.TransitionPhase
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.TransitionPhaseRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.TransitionPhaseResponse'
  /project.v1.ProjectService/UpdateProject:
    post:
      tags:
        - project.v1.ProjectService
      summary: UpdateProject
      operationId: project.v1.ProjectService.UpdateProject
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.UpdateProjectRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.UpdateProjectResponse'
  /project.v1.ProjectService/ValidatePhaseReadiness:
    post:
      tags:
        - project.v1.ProjectService
      summary: ValidatePhaseReadiness
      operationId: project.v1.ProjectService.ValidatePhaseReadiness
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/project.v1.ValidatePhaseReadinessRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/project.v1.ValidatePhaseReadinessResponse'
components:
  schemas:
    connect-protocol-version:
      type: number
      title: Connect-Protocol-Version
      enum:
        - 1
      description: Define the version of the Connect protocol
      const: 1
    connect-timeout-header:
      type: number
      title: Connect-Timeout-Ms
      description: Define the timeout, in ms
    connect.error:
      type: object
      properties:
        code:
          type: string
          examples:
            - not_found
          enum:
            - canceled
            - unknown
            - invalid_argument
            - deadline_exceeded
            - not_found
            - already_exists
            - permission_denied
            - resource_exhausted
            - failed_precondition
            - aborted
            - out_of_range
            - unimplemented
            - internal
            - unavailable
            - data_loss
            - unauthenticated
          description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
        details:
          type: array
          items:
            $ref: '#/components/schemas/connect.error_details.Any'
          description: A list of messages that carry the error details. There is no limit on the number of messages.
      title: Connect Error
      additionalProperties: true
      description: 'Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation'
    connect.error_details.Any:
      type: object
      properties:
        type:
          type: string
          description: 'A URL that acts as a globally unique identifier for the type of the serialized message. For example: `type.googleapis.com/google.rpc.ErrorInfo`. This is used to determine the schema of the data in the `value` field and is the discriminator for the `debug` field.'
        value:
          type: string
          format: binary
          description: The Protobuf message, serialized as bytes and base64-encoded. The specific message type is identified by the `type` field.
        debug:
          oneOf:
            - type: object
              title: Any
              additionalProperties: true
              description: Detailed error information.
          discriminator:
            propertyName: type
          title: Debug
          description: Deserialized error detail payload. The 'type' field indicates the schema. This field is for easier debugging and should not be relied upon for application logic.
      additionalProperties: true
      description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message, with an additional debug field for ConnectRPC error details.
    google.protobuf.FieldMask:
      type: string
      description: |-
        `FieldMask` represents a set of symbolic field paths, for example:

             paths: "f.a"
             paths: "f.b.d"

         Here `f` represents a field in some root message, `a` and `b`
         fields in the message found in `f`, and `d` a field found in the
         message in `f.b`.

         Field masks are used to specify a subset of fields that should be
         returned by a get operation or modified by an update operation.
         Field masks also have a custom JSON encoding (see below).

         # Field Masks in Projections

         When used in the context of a projection, a response message or
         sub-message is filtered by the API to only contain those fields as
         specified in the mask. For example, if the mask in the previous
         example is applied to a response message as follows:

             f {
               a : 22
               b {
                 d : 1
                 x : 2
               }
               y : 13
             }
             z: 8

         The result will not contain specific values for fields x,y and z
         (their value will be set to the default, and omitted in proto text
         output):


             f {
               a : 22
               b {
                 d : 1
               }
             }

         A repeated field is not allowed except at the last position of a
         paths string.

         If a FieldMask object is not present in a get operation, the
         operation applies to all fields (as if a FieldMask of all fields
         had been specified).

         Note that a field mask does not necessarily apply to the
         top-level response message. In case of a REST get operation, the
         field mask applies directly to the response, but in case of a REST
         list operation, the mask instead applies to each individual message
         in the returned resource list. In case of a REST custom method,
         other definitions may be used. Where the mask applies will be
         clearly documented together with its declaration in the API.  In
         any case, the effect on the returned resource/resources is required
         behavior for APIs.

         # Field Masks in Update Operations

         A field mask in update operations specifies which fields of the
         targeted resource are going to be updated. The API is required
         to only change the values of the fields as specified in the mask
         and leave the others untouched. If a resource is passed in to
         describe the updated values, the API ignores the values of all
         fields not covered by the mask.

         If a repeated field is specified for an update operation, new values will
         be appended to the existing repeated field in the target resource. Note that
         a repeated field is only allowed in the last position of a `paths` string.

         If a sub-message is specified in the last position of the field mask for an
         update operation, then new value will be merged into the existing sub-message
         in the target resource.

         For example, given the target message:

             f {
               b {
                 d: 1
                 x: 2
               }
               c: [1]
             }

         And an update message:

             f {
               b {
                 d: 10
               }
               c: [2]
             }

         then if the field mask is:

          paths: ["f.b", "f.c"]

         then the result will be:

             f {
               b {
                 d: 10
                 x: 2
               }
               c: [1, 2]
             }

         An implementation may provide options to override this default behavior for
         repeated and message fields.

         In order to reset a field's value to the default, the field must
         be in the mask and set to the default value in the provided resource.
         Hence, in order to reset all fields of a resource, provide a default
         instance of the resource and set all fields in the mask, or do
         not provide a mask as described below.

         If a field mask is not present on update, the operation applies to
         all fields (as if a field mask of all fields has been specified).
         Note that in the presence of schema evolution, this may mean that
         fields the client does not know and has therefore not filled into
         the request will be reset to their default. If this is unwanted
         behavior, a specific service may require a client to always specify
         a field mask, producing an error if not.

         As with get operations, the location of the resource which
         describes the updated values in the request message depends on the
         operation kind. In any case, the effect of the field mask is
         required to be honored by the API.

         ## Considerations for HTTP REST

         The HTTP kind of an update operation which uses a field mask must
         be set to PATCH instead of PUT in order to satisfy HTTP semantics
         (PUT must only be used for full updates).

         # JSON Encoding of Field Masks

         In JSON, a field mask is encoded as a single string where paths are
         separated by a comma. Fields name in each path are converted
         to/from lower-camel naming conventions.

         As an example, consider the following message declarations:

             message Profile {
               User user = 1;
               Photo photo = 2;
             }
             message User {
               string display_name = 1;
               string address = 2;
             }

         In proto a field mask for `Profile` may look as such:

             mask {
               paths: "user.display_name"
               paths: "photo"
             }

         In JSON, the same mask is represented as below:

             {
               mask: "user.displayName,photo"
             }

         # Field Masks and Oneof Fields

         Field masks treat fields in oneofs just as regular fields. Consider the
         following message:

             message SampleMessage {
               oneof test_oneof {
                 string name = 4;
                 SubMessage sub_message = 9;
               }
             }

         The field mask can be:

             mask {
               paths: "name"
             }

         Or:

             mask {
               paths: "sub_message"
             }

         Note that oneof type names ("test_oneof" in this case) cannot be used in
         paths.

         ## Field Mask Verification

         The implementation of any API method which has a FieldMask type field in the
         request should verify the included field paths, and return an
         `INVALID_ARGUMENT` error if any path is unmappable.
    google.protobuf.Timestamp:
      type: string
      examples:
        - "2023-01-15T01:30:15.01Z"
        - "2024-12-25T12:00:00Z"
      format: date-time
      description: |-
        A Timestamp represents a point in time independent of any time zone or local
         calendar, encoded as a count of seconds and fractions of seconds at
         nanosecond resolution. The count is relative to an epoch at UTC midnight on
         January 1, 1970, in the proleptic Gregorian calendar which extends the
         Gregorian calendar backwards to year one.

         All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
         second table is needed for interpretation, using a [24-hour linear
         smear](https://developers.google.com/time/smear).

         The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
         restricting to that range, we ensure that we can convert to and from [RFC
         3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

         # Examples

         Example 1: Compute Timestamp from POSIX `time()`.

             Timestamp timestamp;
             timestamp.set_seconds(time(NULL));
             timestamp.set_nanos(0);

         Example 2: Compute Timestamp from POSIX `gettimeofday()`.

             struct timeval tv;
             gettimeofday(&tv, NULL);

             Timestamp timestamp;
             timestamp.set_seconds(tv.tv_sec);
             timestamp.set_nanos(tv.tv_usec * 1000);

         Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

             FILETIME ft;
             GetSystemTimeAsFileTime(&ft);
             UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

             // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
             // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
             Timestamp timestamp;
             timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
             timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

         Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

             long millis = System.currentTimeMillis();

             Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
                 .setNanos((int) ((millis % 1000) * 1000000)).build();

         Example 5: Compute Timestamp from Java `Instant.now()`.

             Instant now = Instant.now();

             Timestamp timestamp =
                 Timestamp.newBuilder().setSeconds(now.getEpochSecond())
                     .setNanos(now.getNano()).build();

         Example 6: Compute Timestamp from current time in Python.

             timestamp = Timestamp()
             timestamp.GetCurrentTime()

         # JSON Mapping

         In JSON format, the Timestamp type is encoded as a string in the
         [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
         format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
         where {year} is always expressed using four digits while {month}, {day},
         {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
         seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
         are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
         is required. A proto3 JSON serializer should always use UTC (as indicated by
         "Z") when printing the Timestamp type and a proto3 JSON parser should be
         able to accept both UTC and other timezones (as indicated by an offset).

         For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
         01:30 UTC on January 15, 2017.

         In JavaScript, one can convert a Date object to this format using the
         standard
         [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
         method. In Python, a standard `datetime.datetime` object can be converted
         to this format using
         [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
         the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
         the Joda Time's [`ISODateTimeFormat.dateTime()`](
         http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
         ) to obtain a formatter capable of generating timestamps in this format.
    packages.api.v1.pagination.PaginationRequest:
      type: object
      properties:
        pageOffset:
          type: integer
          title: page_offset
          format: int32
        pageSize:
          type: integer
          title: page_size
          format: int32
        sort:
          type: array
          items:
            type: string
          title: sort
        fields:
          title: fields
          $ref: '#/components/schemas/google.protobuf.FieldMask'
      title: PaginationRequest
      additionalProperties: false
      description: Alias for extension/land modules that reference PaginationRequest
    packages.api.v1.pagination.PaginationResponse:
      type: object
      properties:
        totalCount:
          type: integer
          title: total_count
          format: int32
        pageOffset:
          type: integer
          title: page_offset
          format: int32
        pageSize:
          type: integer
          title: page_size
          format: int32
        hasNext:
          type: boolean
          title: has_next
      title: PaginationResponse
      additionalProperties: false
      description: Paginated list response wrapper
    planning.v1.CommissioningConfirmation:
      type: object
      properties:
        approvedProjectId:
          type: string
          title: approved_project_id
        twinId:
          type: string
          title: twin_id
          description: Reference to provisioned digital twin
        provisionedByActorId:
          type: string
          title: provisioned_by_actor_id
        provisionedAt:
          title: provisioned_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        assetIdentityLinks:
          type: integer
          title: asset_identity_links
          format: int32
          description: Count of linked asset identities
      title: CommissioningConfirmation
      additionalProperties: false
      description: 'CommissioningConfirmation evidence: Twin provisioned and asset identities linked.'
    planning.v1.ElectricalSignoff:
      type: object
      properties:
        layoutId:
          type: string
          title: layout_id
        electricalAnalysisId:
          type: string
          title: electrical_analysis_id
          description: Reference to electrical service validation run
        validatedByActorId:
          type: string
          title: validated_by_actor_id
        validatedAt:
          title: validated_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        violations:
          type: array
          items:
            type: string
          title: violations
          description: Empty if no violations
        electricalFeasibilityScore:
          type: number
          title: electrical_feasibility_score
          format: double
      title: ElectricalSignoff
      additionalProperties: false
      description: 'ElectricalSignoff evidence: Electrical validation passed.'
    planning.v1.LayoutApproval:
      type: object
      properties:
        candidateId:
          type: string
          title: candidate_id
          description: Selected MLCandidate ID
        mlExperimentId:
          type: string
          title: ml_experiment_id
          description: Reference to ML phase output
        approvedByActorId:
          type: string
          title: approved_by_actor_id
        approvedAt:
          title: approved_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        candidateCompositeScore:
          type: number
          title: candidate_composite_score
          format: double
        selectionRationale:
          type: string
          title: selection_rationale
      title: LayoutApproval
      additionalProperties: false
      description: 'LayoutApproval evidence: ML candidate set complete and preferred candidate selected.'
    planning.v1.PlanningAcceptance:
      type: object
      properties:
        boundaryId:
          type: string
          title: boundary_id
          description: Reference to boundary artifact
        acceptedByActorId:
          type: string
          title: accepted_by_actor_id
          description: User or system actor ID
        acceptedAt:
          title: accepted_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        notes:
          type: string
          title: notes
      title: PlanningAcceptance
      additionalProperties: false
      description: PlanningAcceptance marks the boundary and planning inputs as accepted.
    planning.v1.ReviewApproval:
      type: object
      properties:
        transmissionRouteId:
          type: string
          title: transmission_route_id
        lod400ChecklistId:
          type: string
          title: lod_400_checklist_id
          description: Reference to LOD checklist review
        reviewedByActorId:
          type: string
          title: reviewed_by_actor_id
        reviewedAt:
          title: reviewed_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        mandatoryItemsVerified:
          type: integer
          title: mandatory_items_verified
          format: int32
        blockers:
          type: array
          items:
            type: string
          title: blockers
          description: Empty if all blockers resolved
        qualityScore:
          type: number
          title: quality_score
          format: double
          description: '[0, 1]'
      title: ReviewApproval
      additionalProperties: false
      description: 'ReviewApproval evidence: LOD 400 checklist passed + quality gate cleared.'
    planning.v1.StakeholderApproval:
      type: object
      properties:
        reviewApprovedLayoutId:
          type: string
          title: review_approved_layout_id
        approvedByActorId:
          type: string
          title: approved_by_actor_id
          description: Stakeholder user ID
        actorRole:
          type: string
          title: actor_role
          description: e.g., "designer", "manager", "owner"
        approvedAt:
          title: approved_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        approvalNotes:
          type: string
          title: approval_notes
      title: StakeholderApproval
      additionalProperties: false
      description: 'StakeholderApproval evidence: Manager/stakeholder approval for commissioning.'
    planning.v1.TransitionEvidence:
      type: object
      oneOf:
        - type: object
          properties:
            commissioningConfirmation:
              title: commissioning_confirmation
              $ref: '#/components/schemas/planning.v1.CommissioningConfirmation'
          title: commissioning_confirmation
          required:
            - commissioningConfirmation
        - type: object
          properties:
            electricalSignoff:
              title: electrical_signoff
              $ref: '#/components/schemas/planning.v1.ElectricalSignoff'
          title: electrical_signoff
          required:
            - electricalSignoff
        - type: object
          properties:
            layoutApproval:
              title: layout_approval
              $ref: '#/components/schemas/planning.v1.LayoutApproval'
          title: layout_approval
          required:
            - layoutApproval
        - type: object
          properties:
            planningAcceptance:
              title: planning_acceptance
              $ref: '#/components/schemas/planning.v1.PlanningAcceptance'
          title: planning_acceptance
          required:
            - planningAcceptance
        - type: object
          properties:
            reviewApproval:
              title: review_approval
              $ref: '#/components/schemas/planning.v1.ReviewApproval'
          title: review_approval
          required:
            - reviewApproval
        - type: object
          properties:
            stakeholderApproval:
              title: stakeholder_approval
              $ref: '#/components/schemas/planning.v1.StakeholderApproval'
          title: stakeholder_approval
          required:
            - stakeholderApproval
        - type: object
          properties:
            transmissionSignoff:
              title: transmission_signoff
              $ref: '#/components/schemas/planning.v1.TransmissionSignoff'
          title: transmission_signoff
          required:
            - transmissionSignoff
      title: TransitionEvidence
      additionalProperties: false
      description: TransitionEvidence wraps all possible evidence types.
    planning.v1.TransmissionSignoff:
      type: object
      properties:
        electricalLayoutId:
          type: string
          title: electrical_layout_id
        transmissionRouteId:
          type: string
          title: transmission_route_id
          description: Reference to transmission service route
        approvedByActorId:
          type: string
          title: approved_by_actor_id
        approvedAt:
          title: approved_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        protectionDevices:
          type: array
          items:
            type: string
          title: protection_devices
        faultIsolationPoints:
          type: integer
          title: fault_isolation_points
          format: int32
      title: TransmissionSignoff
      additionalProperties: false
      description: 'TransmissionSignoff evidence: Transmission routing and protection complete.'
    planning.v1.WorkflowPhase:
      type: string
      title: WorkflowPhase
      enum:
        - WORKFLOW_PHASE_UNSPECIFIED
        - PLANNING
        - LAYOUT_READY
        - ELECTRICAL_READY
        - TRANSMISSION_READY
        - REVIEW_READY
        - APPROVED
        - COMMISSIONING_READY
        - ARCHIVED
    project.v1.CreateProjectRequest:
      type: object
      properties:
        name:
          type: string
          title: name
        description:
          type: string
          title: description
        metadata:
          title: metadata
          $ref: '#/components/schemas/project.v1.ProjectMetadata'
      title: CreateProjectRequest
      additionalProperties: false
    project.v1.CreateProjectResponse:
      type: object
      properties:
        project:
          title: project
          $ref: '#/components/schemas/project.v1.Project'
      title: CreateProjectResponse
      additionalProperties: false
    project.v1.DeleteProjectRequest:
      type: object
      properties:
        id:
          type: string
          title: id
      title: DeleteProjectRequest
      additionalProperties: false
    project.v1.DeleteProjectResponse:
      type: object
      title: DeleteProjectResponse
      additionalProperties: false
    project.v1.GetPhaseStateRequest:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
      title: GetPhaseStateRequest
      additionalProperties: false
    project.v1.GetPhaseStateResponse:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        currentPhase:
          title: current_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        phaseEnteredAt:
          title: phase_entered_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        lastTransition:
          title: last_transition
          $ref: '#/components/schemas/project.v1.PhaseTransitionHistoryEntry'
      title: GetPhaseStateResponse
      additionalProperties: false
    project.v1.GetProjectRequest:
      type: object
      properties:
        id:
          type: string
          title: id
      title: GetProjectRequest
      additionalProperties: false
    project.v1.GetProjectResponse:
      type: object
      properties:
        project:
          title: project
          $ref: '#/components/schemas/project.v1.Project'
      title: GetProjectResponse
      additionalProperties: false
    project.v1.ListPhaseTransitionsRequest:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        pagination:
          title: pagination
          $ref: '#/components/schemas/packages.api.v1.pagination.PaginationRequest'
      title: ListPhaseTransitionsRequest
      additionalProperties: false
    project.v1.ListPhaseTransitionsResponse:
      type: object
      properties:
        transitions:
          type: array
          items:
            $ref: '#/components/schemas/project.v1.PhaseTransitionHistoryEntry'
          title: transitions
        pagination:
          title: pagination
          $ref: '#/components/schemas/packages.api.v1.pagination.PaginationResponse'
      title: ListPhaseTransitionsResponse
      additionalProperties: false
    project.v1.ListProjectsRequest:
      type: object
      properties:
        statusFilter:
          title: status_filter
          $ref: '#/components/schemas/project.v1.ProjectStatus'
        pagination:
          title: pagination
          $ref: '#/components/schemas/packages.api.v1.pagination.PaginationRequest'
      title: ListProjectsRequest
      additionalProperties: false
    project.v1.ListProjectsResponse:
      type: object
      properties:
        projects:
          type: array
          items:
            $ref: '#/components/schemas/project.v1.Project'
          title: projects
        pagination:
          title: pagination
          $ref: '#/components/schemas/packages.api.v1.pagination.PaginationResponse'
      title: ListProjectsResponse
      additionalProperties: false
    project.v1.PhaseTransitionHistoryEntry:
      type: object
      properties:
        id:
          type: string
          title: id
        fromPhase:
          title: from_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        toPhase:
          title: to_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        occurredAt:
          title: occurred_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        actorId:
          type: string
          title: actor_id
        reason:
          type: string
          title: reason
        isRollback:
          type: boolean
          title: is_rollback
        rollbackReason:
          type: string
          title: rollback_reason
      title: PhaseTransitionHistoryEntry
      additionalProperties: false
    project.v1.Project:
      type: object
      properties:
        id:
          type: string
          title: id
        name:
          type: string
          title: name
        description:
          type: string
          title: description
        status:
          title: status
          $ref: '#/components/schemas/project.v1.ProjectStatus'
        site:
          title: site
          $ref: '#/components/schemas/project.v1.Site'
        metadata:
          title: metadata
          $ref: '#/components/schemas/project.v1.ProjectMetadata'
        createdAt:
          title: created_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        updatedAt:
          title: updated_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        workflowPhase:
          title: workflow_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        phaseEnteredAt:
          title: phase_entered_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        transitionHistory:
          type: array
          items:
            $ref: '#/components/schemas/project.v1.PhaseTransitionHistoryEntry'
          title: transition_history
        activeBlockers:
          type: array
          items:
            type: string
          title: active_blockers
      title: Project
      additionalProperties: false
    project.v1.ProjectMetadata:
      type: object
      properties:
        targetCapacityMw:
          type: number
          title: target_capacity_mw
          format: double
        locationName:
          type: string
          title: location_name
        clientName:
          type: string
          title: client_name
        notes:
          type: string
          title: notes
      title: ProjectMetadata
      additionalProperties: false
    project.v1.ProjectStatus:
      type: string
      title: ProjectStatus
      enum:
        - PROJECT_STATUS_UNSPECIFIED
        - PROJECT_STATUS_DRAFT
        - PROJECT_STATUS_DESIGN
        - PROJECT_STATUS_SIMULATION
        - PROJECT_STATUS_REVIEW
        - PROJECT_STATUS_APPROVED
        - PROJECT_STATUS_ARCHIVED
    project.v1.Site:
      type: object
      properties:
        id:
          type: string
          title: id
        projectId:
          type: string
          title: project_id
        name:
          type: string
          title: name
        boundaryGeojson:
          type: string
          title: boundary_geojson
          description: GeoJSON polygon boundary
        areaSqm:
          type: number
          title: area_sqm
          format: double
        latitude:
          type: number
          title: latitude
          format: double
        longitude:
          type: number
          title: longitude
          format: double
        timezone:
          type: string
          title: timezone
        createdAt:
          title: created_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: Site
      additionalProperties: false
    project.v1.TransitionPhaseRequest:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        targetPhase:
          title: target_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        evidence:
          title: evidence
          $ref: '#/components/schemas/planning.v1.TransitionEvidence'
        actorId:
          type: string
          title: actor_id
        reason:
          type: string
          title: reason
      title: TransitionPhaseRequest
      additionalProperties: false
    project.v1.TransitionPhaseResponse:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        previousPhase:
          title: previous_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        currentPhase:
          title: current_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        transitionRecord:
          title: transition_record
          $ref: '#/components/schemas/project.v1.PhaseTransitionHistoryEntry'
        wasNoop:
          type: boolean
          title: was_noop
      title: TransitionPhaseResponse
      additionalProperties: false
    project.v1.UpdateProjectRequest:
      type: object
      properties:
        id:
          type: string
          title: id
        name:
          type: string
          title: name
        description:
          type: string
          title: description
        status:
          title: status
          $ref: '#/components/schemas/project.v1.ProjectStatus'
        metadata:
          title: metadata
          $ref: '#/components/schemas/project.v1.ProjectMetadata'
      title: UpdateProjectRequest
      additionalProperties: false
    project.v1.UpdateProjectResponse:
      type: object
      properties:
        project:
          title: project
          $ref: '#/components/schemas/project.v1.Project'
      title: UpdateProjectResponse
      additionalProperties: false
    project.v1.ValidatePhaseReadinessRequest:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        targetPhase:
          title: target_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        evidence:
          title: evidence
          $ref: '#/components/schemas/planning.v1.TransitionEvidence'
      title: ValidatePhaseReadinessRequest
      additionalProperties: false
    project.v1.ValidatePhaseReadinessResponse:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
        currentPhase:
          title: current_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        targetPhase:
          title: target_phase
          $ref: '#/components/schemas/planning.v1.WorkflowPhase'
        isValid:
          type: boolean
          title: is_valid
        blockerReasons:
          type: array
          items:
            type: string
          title: blocker_reasons
        canTransitionAfter:
          title: can_transition_after
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: ValidatePhaseReadinessResponse
      additionalProperties: false
security: []
tags:
  - name: project.v1.ProjectService
